package jwc;

/** Decodes WAV, MP3, MP4, and OGG audio files. */
public class Decoder {

    // need to decode audio files to read actual audio data/transients?

    public static byte[] data() {
        return new byte[0];
    }

}